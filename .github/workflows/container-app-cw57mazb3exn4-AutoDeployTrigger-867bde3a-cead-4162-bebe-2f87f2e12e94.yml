name: Trigger auto deployment for container-app-cw57mazb3exn4

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/container-app-cw57mazb3exn4-AutoDeployTrigger-867bde3a-cead-4162-bebe-2f87f2e12e94.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPCW57MAZB3EXN4_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPCW57MAZB3EXN4_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPCW57MAZB3EXN4_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: containerregcw57mazb3exn4.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPCW57MAZB3EXN4_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPCW57MAZB3EXN4_REGISTRY_PASSWORD }}
          containerAppName: container-app-cw57mazb3exn4
          resourceGroup: ResourceGroup1
          imageToBuild: containerregcw57mazb3exn4.azurecr.io/container-app-cw57mazb3exn4:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


